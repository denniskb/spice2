{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [{
            "label": "Release",
            "type": "shell",
            "command": "mkdir -p build/release && cd build/release && cmake ../.. && make -j 8",
            "group": "build",
            "problemMatcher": "[]",
            "presentation": {
                "focus": true,
                "clear": true
            }
        },{
			"label": "Run",
            "type": "shell",
            "command": "./build/release/spice",
            "group": "test",
			"dependsOn": "Release",
            "problemMatcher": "[]",
            "presentation": {
                "focus": true,
                "clear": true
            }
		}
    ]
}